import React from 'react';
import './nav.scss';

const Nav = () => {
    return (
        <nav style={{display: 'flex', justifyContent: 'flex-end'}}>
            <p>tekst</p>
        </nav>
    );
}

export default Nav;